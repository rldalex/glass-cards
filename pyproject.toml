[project]
name = "glass-cards-backend"
version = "0.0.1"
requires-python = ">=3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = [".", "tests/ha_stubs"]

[tool.mypy]
python_version = "3.13"
strict = true
