import { baseStyles } from './base';
import { tabStyles } from './tabs';
import { formStyles } from './forms';
import { sectionStyles } from './sections';

export { baseStyles, tabStyles, formStyles, sectionStyles };

export const configPanelStyles = [
  baseStyles,
  tabStyles,
  formStyles,
  sectionStyles,
];
